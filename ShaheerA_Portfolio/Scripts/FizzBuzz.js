// JavaScript source code
function FizzBuzz() {
    let fbArray = new Array(), entry = "", fizzCase = 0, buzzCase = 0;
    const CRLF = '\r\n';
    for (let loop = 1; loop <= 100; loop++) {
        fizzCase = loop % num1;
        buzzCase = loop % num2;
        if (!fizzCase && !buzzCase) {
        }
        else if (!fizzCase) {
        }
        else if (!buzzCase) {
        }
        else {
        }
        return fbArray.join(', ');
    }
}
